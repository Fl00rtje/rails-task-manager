<h1>Edit your task:</h1>

<%= form_for(@task) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>

  <%= t.label :details %>
  <%= t.text_field :details %>

  <%= t.submit %>
  <!-- <%= link_to t.submit, tasks_path %> -->
<% end %>
